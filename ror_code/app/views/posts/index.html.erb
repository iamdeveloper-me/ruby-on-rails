<% @posts.each do |post| %>
	<%= post.title %>
	<%= post.description %>
	<%= link_to "Show", post_path(post) %>
	<%= link_to "Destroy", post_path(post), data: {:confirm => 'Are you sure?'}, :method => :delete  %>
	</br>
<% end %>

